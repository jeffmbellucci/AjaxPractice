<%= form_for @secret, :url => user_secrets_url(@recipient_id) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>

  <%= f.hidden_field :author_id, value: current_user.id %>
  <br>

  <%= f.hidden_field :recipient_id, value: @recipient_id %>
  <br>

  <%= f.submit %>

<% end %>